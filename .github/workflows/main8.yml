name: FGO Login System 8

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
 # schedule:
  #  - cron: "07 00 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]

    steps:
      - name: Install Requeriments
        run: pip install -r https://raw.githubusercontent.com/DNNDHH/F-D-L/master/requirements.txt

      - name: Gettings files from Repo
        uses: actions/checkout@v2

      - name: Execute main script 1
        run: |
          python3 main.py
        env:
          userIds: ${{ secrets.GAME_USERIDS_8 }}
          authKeys: ${{ secrets.GAME_AUTHKEYS_8 }}
          secretKeys: ${{ secrets.GAME_SECRETKEYS_8 }}
          UserAgent: ${{ secrets.GAME_USERAGENT }}
          fateRegion: ${{ secrets.GAME_REGION }}
          webhookDiscord: ${{ secrets.DISCORD_WEBHOOK_6 }}
